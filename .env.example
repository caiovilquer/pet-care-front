# Variáveis de Ambiente - Copie este arquivo para .env e configure suas chaves

# URL da API backend
# Desenvolvimento com proxy: use '/api/v1' (recomendado)
# Desenvolvimento sem proxy: use 'http://localhost:8080/api/v1'
# Produção: use a URL completa da sua API
API_URL=/api/v1

# Chave da API do Google Maps
# Obtenha sua chave em: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=sua-chave-real-do-google-maps-aqui

# Configuração do Proxy (para desenvolvimento)
# O proxy redireciona /api/* para http://localhost:8080
# Se seu backend usar porta diferente, edite proxy.conf.json
